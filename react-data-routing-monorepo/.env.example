# ─── API Configuration ────────────────────────────────────
# Base URL for the backend API
VITE_API_BASE_URL=http://localhost:3001/api

# Enable mock API mode (no backend required — uses per-module mock data)
VITE_API_MOCK=true

# ─── Development ──────────────────────────────────────────
# Auto-login with mock admin user during development (skip login screen)
VITE_AUTO_LOGIN=true
